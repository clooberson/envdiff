{
  "name": "envdiff",
  "version": "0.1.0",
  "description": "Utility to compare .env files across environments and flag missing or mismatched keys",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "env",
    "dotenv",
    "diff",
    "environment",
    "cli"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/*.test.js"
    ]
  }
}
